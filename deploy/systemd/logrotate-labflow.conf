/var/log/labflow-deploy.log {
    # /var/log e root:syslog com permissao de grupo; sem `su` o logrotate se
    # recusa a rodar por considerar o diretorio pai inseguro.
    su root syslog
    monthly
    rotate 12
    compress
    missingok
    notifempty
}
